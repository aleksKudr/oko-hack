#!/usr/bin/env python
# -*- coding: utf-8 -*-

import serial, time
ser = serial.Serial("/dev/ttyACM0", 9600)
while True:
	for i in range(0,255)
     write("What you want to send"+str(i))
     time.sleep(1)
